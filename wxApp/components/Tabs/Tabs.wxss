/* components/Tabs/Tabs.wxss */
.tabs .tabs_title {
  display: flex;
}
.tabs .tabs_title view {
  display: flex;
  justify-content: center;
  align-items: center;
  flex: 1;
  padding: 15rpx 0px;
}
.tabs .tabs_title .active {
  color: var(--themeColor);
  border-bottom: 1px solid var(--themeColor);
}
